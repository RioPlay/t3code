diff --git a/android/CMakeLists.txt b/android/CMakeLists.txt
index 005caaf255529389c2684dbe8fbf0cb099d80e97..631378c16f09c4132cd15ac4deeebdd1e682c85a 100644
--- a/android/CMakeLists.txt
+++ b/android/CMakeLists.txt
@@ -3,13 +3,17 @@ project(ReactNativeShikiEngine)
 
 set(CMAKE_VERBOSE_MAKEFILE ON)
 
-# Find prebuilt oniguruma library
+# Find prebuilt oniguruma library. Use an explicit imported target so Linux host
+# /usr/lib/libonig.so is never picked up during Android cross-compiles.
 find_library(LOG_LIB log)
-find_library(ONIG_LIB onig
-    PATHS ${CMAKE_CURRENT_SOURCE_DIR}/src/main/jniLibs/${ANDROID_ABI}
-    NO_CMAKE_FIND_ROOT_PATH
-    REQUIRED
-)
+
+set(ONIG_LIB_PATH "${CMAKE_CURRENT_SOURCE_DIR}/src/main/jniLibs/${ANDROID_ABI}/libonig.so")
+if(NOT EXISTS "${ONIG_LIB_PATH}")
+    message(FATAL_ERROR "onig library not found at ${ONIG_LIB_PATH}")
+endif()
+
+add_library(onig SHARED IMPORTED GLOBAL)
+set_target_properties(onig PROPERTIES IMPORTED_LOCATION "${ONIG_LIB_PATH}")
 
 # Main library
 add_library(react-native-shiki-engine SHARED
@@ -65,7 +69,7 @@ find_package(ReactAndroid REQUIRED CONFIG)
 
 # Link libraries
 target_link_libraries(react-native-shiki-engine
-    ${ONIG_LIB}
+    onig
     ${LOG_LIB}
     android
     fbjni::fbjni
