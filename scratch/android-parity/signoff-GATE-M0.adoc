= GATE-M0 — Staging E2E Sign-off
:doctype: article

Human approval for step **s09**. Agent must not start **s10** until this is signed.

Runbook: `scratch/android-parity/STAGING-RUNBOOK.md`

== Checklist (all must pass)

* [ ] Relay deployed to staging with `RELAY_FCM_DELIVERY_ENABLED=true`
* [ ] Android physical device: signed in, environment linked
* [ ] Device row visible in relay admin/debug
* [ ] Synthetic `waiting_for_approval` → FCM push received (relay-generated, not local mock)
* [ ] Notification tap → correct thread (cold start OK)
* [ ] `smoke-matrix.adoc` §M0 rows marked PASS
* [ ] `loop-state.json` → `"gates": { "m0_staging": "pass" }`

== Sign-off

* Date:
* Device:
* Relay staging URL:
* Git SHA (fork main):
* Signed by:

Then reply to agent: **gate pass**