= GATE-T1 — Control-plane baseline (step s16)
:doctype: article

== Criteria

* [ ] `smoke-matrix.adoc` T1 rows PASS or honest DEGRADED
* [ ] Maestro smoke-launch + smoke-review green (smoke-agent-push if staging harness live)
* [ ] `vp check` + CI mobile-qa jobs green on fork main

== Sign-off

* Date:
* SHA:
* Signed by:

Set `loop-state.json` → `"gates": { "t1_complete": "pass" }`